{
  "servers": {
    "pr-review-mcp": {
      "type": "stdio",
      "command": "python3",
      "args": ["${workspaceFolder}/mcp_server/server.py"],
      "env": {
        "GITHUB_TOKEN": "${config:prReviewMcp.githubToken}",
        "GITHUB_API_BASE": "${config:prReviewMcp.githubApiBase}",
        "GITHUB_GRAPHQL_URL": "${config:prReviewMcp.githubGraphqlUrl}"
      }
    }
  }
}
